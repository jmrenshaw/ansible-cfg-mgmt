all:
  children:
    linux:
      vars:
        ansible_connection: ssh
        ansible_ssh_pass: <password>
        ansible_user: <user>
        ansible_ssh_common_args: -o StrictHostKeyChecking=no
  vars:
    dir: <directory name to store configuration files in>
    gitpath: "<path to repository>"
